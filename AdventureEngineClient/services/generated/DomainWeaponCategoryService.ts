//*** CAUTION ***
//This is an autogenerated file produced by regenerateFrontendServices.py.
//Edits made here will not persist after regeneration.

import { type DomainWeaponCategory } from "../../types/appTypes/appTypes";
import { type FilterCollection, produceFilterParamsFromExpression, validateApiResponse} from "../utils";

export async function getDomainWeaponCategorys(filters?: FilterCollection<DomainWeaponCategory>): Promise<DomainWeaponCategory[]> {
   try {
      const filterString = filters instanceof Array && filters?.length > 0? "?" + produceFilterParamsFromExpression(filters) : "";
      const response = await fetch("http://localhost:8080/getDomainWeaponCategorys" + filterString, { credentials: "include" });
      const errorResponse = await validateApiResponse(response);
      if (typeof errorResponse === "string") {
         throw errorResponse;
      }
      const returnObj = await response.json() as unknown as Array<DomainWeaponCategory>;
      return returnObj;
   }
   catch (errors) {
      throw errors
   }
}

export async function getDomainWeaponCategorybyId(id: number): Promise<DomainWeaponCategory> {
   try {
      const response = await fetch("http://localhost:8080/getDomainWeaponCategory/" + id, { credentials: "include" });
      const errorResponse = await validateApiResponse(response);
      if (typeof errorResponse === "string") {
         throw errorResponse;
      }
      const returnObj = await response.json() as unknown as DomainWeaponCategory;
      return returnObj;
   }
   catch (errors) {
      throw errors
   }
}

export async function saveDomainWeaponCategory<T extends DomainWeaponCategory | DomainWeaponCategory[]>(obj: T): Promise<T> {
   try {
      const response = await fetch("http://localhost:8080/saveDomainWeaponCategory", {method: "POST", headers: {"Content-Type": "application/json"}, body: JSON.stringify(obj), credentials: "include" });
      const errorResponse = await validateApiResponse(response);
      if (typeof errorResponse === "string") {
         throw errorResponse;
      }
      const returnObj = await response.json() as unknown as T;
      return returnObj;
   }
   catch (errors) {
      throw errors
   }
}

