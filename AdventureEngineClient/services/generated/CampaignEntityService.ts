//*** CAUTION ***
//This is an autogenerated file produced by regenerateFrontendServices.py.
//Edits made here will not persist after regeneration.

import { type CampaignEntity } from "../../types/appTypes/appTypes";
import { type FilterCollection, produceFilterParamsFromExpression, validateApiResponse} from "../utils";

export async function getCampaignEntitys(filters?: FilterCollection<CampaignEntity>): Promise<CampaignEntity[]> {
   try {
      const filterString = filters instanceof Array && filters?.length > 0? "?" + produceFilterParamsFromExpression(filters) : "";
      const response = await fetch("http://localhost:8080/getCampaignEntitys" + filterString, { credentials: "include" });
      const errorResponse = await validateApiResponse(response);
      if (typeof errorResponse === "string") {
         throw errorResponse;
      }
      const returnObj = await response.json() as unknown as Array<CampaignEntity>;
      return returnObj;
   }
   catch (errors) {
      throw errors
   }
}

export async function getCampaignEntitybyId(id: number): Promise<CampaignEntity> {
   try {
      const response = await fetch("http://localhost:8080/getCampaignEntity/" + id, { credentials: "include" });
      const errorResponse = await validateApiResponse(response);
      if (typeof errorResponse === "string") {
         throw errorResponse;
      }
      const returnObj = await response.json() as unknown as CampaignEntity;
      return returnObj;
   }
   catch (errors) {
      throw errors
   }
}

export async function saveCampaignEntity<T extends CampaignEntity | CampaignEntity[]>(obj: T): Promise<T> {
   try {
      const response = await fetch("http://localhost:8080/saveCampaignEntity", {method: "POST", headers: {"Content-Type": "application/json"}, body: JSON.stringify(obj), credentials: "include" });
      const errorResponse = await validateApiResponse(response);
      if (typeof errorResponse === "string") {
         throw errorResponse;
      }
      const returnObj = await response.json() as unknown as T;
      return returnObj;
   }
   catch (errors) {
      throw errors
   }
}

