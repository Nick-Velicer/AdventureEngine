//*** CAUTION ***
//This is an autogenerated file produced by regenerateBackend.py.
//Edits made here will not persist after backend regeneration.

package generatedControllers
import (
   "errors"
   "fmt"
   "github.com/gin-gonic/gin/binding"
   "net/http"
   "regexp"
   "strconv"
   contextProviders "AdventureEngineServer/contextProviders"
   types "AdventureEngineServer/generatedDatabaseTypes"
   dtos "AdventureEngineServer/generatedDTOs"
   utils "AdventureEngineServer/utils"
)

func GetDomainClassResources(context *contextProviders.GeneratedControllerContext[types.DomainClassResource, dtos.DomainClassResourceDTO, contextProviders.GetArgs[types.DomainClassResource], contextProviders.GetReturn[types.DomainClassResource]]) {
   if context == nil {
      panic("No controller context provided")
   }
   queryParams := context.RequestContext.Request.URL.Query()
   
   //Since we can have multiple filters, that sometimes doesn't play nicely with
   //Gin's parameter pulling, so they need to be isolated manually.
   re := regexp.MustCompile(`filter\[`)
   filterParams := make(map[string]string)
   for key, value := range queryParams {
      if re.MatchString(key) && len(value) == 1 {
         filterParams[key] = value[0]
      } else {
         fmt.Println("Unexpected filter configuration for " + key + ", skipping:")
         fmt.Println(value)
      }
   }
   
   parsedFilters := utils.ParseFilterURLExpression(filterParams)
   
   serviceBuffer, err := context.ServiceAction(&contextProviders.GetArgs[types.DomainClassResource]{ Filters: &parsedFilters })
   if err != nil {
      context.RequestContext.IndentedJSON(http.StatusInternalServerError, err.Error())
      return
   }
   
   var returnBuffer []dtos.DomainClassResourceDTO
   for _, dbTypeInstance := range serviceBuffer {
      pointerToDTO, err := context.DTOConverter(&dbTypeInstance)
      if (err != nil) {
         context.RequestContext.IndentedJSON(http.StatusInternalServerError, err.Error())
         return
      }
      
      if (pointerToDTO == nil) {
         context.RequestContext.IndentedJSON(http.StatusInternalServerError, errors.New("DTO conversion resulted in nil for object of type DomainClassResource"))
         return
      }
      
      returnBuffer = append(returnBuffer, *pointerToDTO)
   }
   context.RequestContext.IndentedJSON(http.StatusOK, returnBuffer)
}

func GetDomainClassResourceById(context *contextProviders.GeneratedControllerContext[types.DomainClassResource, dtos.DomainClassResourceDTO, contextProviders.GetByIdArgs[types.DomainClassResource], contextProviders.GetByIdReturn[types.DomainClassResource]]) {
   if context == nil {
      panic("No controller context provided")
   }
   
   id := context.RequestContext.Param("id")
   idNum, err := strconv.Atoi(id)
   if err != nil {
      context.RequestContext.IndentedJSON(http.StatusInternalServerError, err.Error())
      return
   }
   serviceBuffer, err := context.ServiceAction(&contextProviders.GetByIdArgs[types.DomainClassResource]{ Id: idNum })
   if err != nil {
      context.RequestContext.IndentedJSON(http.StatusInternalServerError, err.Error())
      return
   }
   
   if serviceBuffer == nil {
      context.RequestContext.IndentedJSON(http.StatusOK, nil)
      return
   }
   
   returnBuffer, err := context.DTOConverter(serviceBuffer)
   
   if err != nil {
      context.RequestContext.IndentedJSON(http.StatusInternalServerError, err.Error())
      return
   }
   
   if (returnBuffer == nil) {
      context.RequestContext.IndentedJSON(http.StatusInternalServerError, errors.New("DTO conversion resulted in nil for object of type DomainClassResource"))
      return
   }
   context.RequestContext.IndentedJSON(http.StatusOK, returnBuffer)
}

func SaveDomainClassResource(context *contextProviders.GeneratedControllerContext[types.DomainClassResource, dtos.DomainClassResourceDTO, contextProviders.SaveArgs[types.DomainClassResource], contextProviders.SaveReturn[types.DomainClassResource]]) {
   if context == nil {
      panic("No controller context provided")
   }
   
   //Weirdness with unmarshalling, cannot unmarshal into a nil pointer, there must be some pre-initialization somewhere along the line
   var DTOBuffer *dtos.DomainClassResourceDTO = &dtos.DomainClassResourceDTO{}
   var batchDTOBuffer []*dtos.DomainClassResourceDTO
   var serviceBuffer []*types.DomainClassResource
   
   //If neither a single item nor a collection can be bound to JSON, fail early
   //ShouldBindBodyWith is used instead of ShouldBindJSON since the latter prevents multiple bind attempts
   if err := context.RequestContext.ShouldBindBodyWith(DTOBuffer, binding.JSON); err == nil {
      
      serviceBuffer = []*types.DomainClassResource{context.DTOFlattener(DTOBuffer)}
      serviceReturn, err := context.ServiceAction(&contextProviders.SaveArgs[types.DomainClassResource]{ Items: serviceBuffer })
      if err != nil {
         context.RequestContext.IndentedJSON(http.StatusInternalServerError, err.Error())
         return
      }
      
      returnBuffer, err := context.DTOConverter(serviceReturn[0])
      if err != nil {
         context.RequestContext.IndentedJSON(http.StatusInternalServerError, err.Error())
         return
      }
      
      if (returnBuffer == nil) {
         context.RequestContext.IndentedJSON(http.StatusInternalServerError, errors.New("DTO conversion resulted in nil for object of type DomainClassResource"))
         return
      }
      
      context.RequestContext.IndentedJSON(http.StatusOK, returnBuffer)
      return
      
   } else if err := context.RequestContext.ShouldBindBodyWith(&batchDTOBuffer, binding.JSON); err == nil {
      
      serviceBuffer = utils.Map(batchDTOBuffer, func(dto *dtos.DomainClassResourceDTO) *types.DomainClassResource { return context.DTOFlattener(dto) })
      serviceReturn, err := context.ServiceAction(&contextProviders.SaveArgs[types.DomainClassResource]{ Items: serviceBuffer })
      if err != nil {
         context.RequestContext.IndentedJSON(http.StatusInternalServerError, err.Error())
         return
      }
      
      returnBuffer, err := utils.ErrorCompatibleMap(serviceReturn, func(dbReturn *types.DomainClassResource) (*dtos.DomainClassResourceDTO, error) { return context.DTOConverter(dbReturn) })
      
      if err != nil {
         context.RequestContext.IndentedJSON(http.StatusInternalServerError, err.Error())
         return
      }
      
      context.RequestContext.IndentedJSON(http.StatusOK, returnBuffer)
      return
      
   } else {
      context.RequestContext.IndentedJSON(http.StatusBadRequest, err.Error())
      return
   }
}
