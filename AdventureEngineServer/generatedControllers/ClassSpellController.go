//*** CAUTION ***
//This is an autogenerated file produced by regenerateBackend.py.
//Edits made here will not persist after backend regeneration.

package generatedControllers
import (
   "errors"
   "fmt"
   "github.com/gin-gonic/gin/binding"
   "net/http"
   "regexp"
   "strconv"
   contextProviders "AdventureEngineServer/contextProviders"
   types "AdventureEngineServer/generatedDatabaseTypes"
   dtos "AdventureEngineServer/generatedDTOs"
   utils "AdventureEngineServer/utils"
)

func GetClassSpells(context *contextProviders.GeneratedControllerContext[types.ClassSpell, dtos.ClassSpellDTO, contextProviders.GetArgs[types.ClassSpell], contextProviders.GetReturn[types.ClassSpell]]) {
   if context == nil {
      panic("No controller context provided")
   }
   queryParams := context.RequestContext.Request.URL.Query()
   
   //Since we can have multiple filters, that sometimes doesn't play nicely with
   //Gin's parameter pulling, so they need to be isolated manually.
   re := regexp.MustCompile(`filter\[`)
   filterParams := make(map[string]string)
   for key, value := range queryParams {
      if re.MatchString(key) && len(value) == 1 {
         filterParams[key] = value[0]
      } else {
         fmt.Println("Unexpected filter configuration for " + key + ", skipping:")
         fmt.Println(value)
      }
   }
   
   parsedFilters := utils.ParseFilterURLExpression(filterParams)
   
   serviceBuffer, err := context.ServiceAction(&contextProviders.GetArgs[types.ClassSpell]{ Filters: &parsedFilters })
   if err != nil {
      context.RequestContext.IndentedJSON(http.StatusInternalServerError, err.Error())
      return
   }
   
   var returnBuffer []dtos.ClassSpellDTO
   for _, dbTypeInstance := range serviceBuffer {
      pointerToDTO, err := context.DTOConverter(&dbTypeInstance)
      if (err != nil) {
         context.RequestContext.IndentedJSON(http.StatusInternalServerError, err.Error())
         return
      }
      
      if (pointerToDTO == nil) {
         context.RequestContext.IndentedJSON(http.StatusInternalServerError, errors.New("DTO conversion resulted in nil for object of type ClassSpell"))
         return
      }
      
      returnBuffer = append(returnBuffer, *pointerToDTO)
   }
   context.RequestContext.IndentedJSON(http.StatusOK, returnBuffer)
}

func GetClassSpellById(context *contextProviders.GeneratedControllerContext[types.ClassSpell, dtos.ClassSpellDTO, contextProviders.GetByIdArgs[types.ClassSpell], contextProviders.GetByIdReturn[types.ClassSpell]]) {
   if context == nil {
      panic("No controller context provided")
   }
   
   id := context.RequestContext.Param("id")
   idNum, err := strconv.Atoi(id)
   if err != nil {
      context.RequestContext.IndentedJSON(http.StatusInternalServerError, err.Error())
      return
   }
   serviceBuffer, err := context.ServiceAction(&contextProviders.GetByIdArgs[types.ClassSpell]{ Id: idNum })
   if err != nil {
      context.RequestContext.IndentedJSON(http.StatusInternalServerError, err.Error())
      return
   }
   
   if serviceBuffer == nil {
      context.RequestContext.IndentedJSON(http.StatusOK, nil)
      return
   }
   
   returnBuffer, err := context.DTOConverter(serviceBuffer)
   
   if err != nil {
      context.RequestContext.IndentedJSON(http.StatusInternalServerError, err.Error())
      return
   }
   
   if (returnBuffer == nil) {
      context.RequestContext.IndentedJSON(http.StatusInternalServerError, errors.New("DTO conversion resulted in nil for object of type ClassSpell"))
      return
   }
   context.RequestContext.IndentedJSON(http.StatusOK, returnBuffer)
}

func SaveClassSpell(context *contextProviders.GeneratedControllerContext[types.ClassSpell, dtos.ClassSpellDTO, contextProviders.SaveArgs[types.ClassSpell], contextProviders.SaveReturn[types.ClassSpell]]) {
   if context == nil {
      panic("No controller context provided")
   }
   
   //Weirdness with unmarshalling, cannot unmarshal into a nil pointer, there must be some pre-initialization somewhere along the line
   var DTOBuffer *dtos.ClassSpellDTO = &dtos.ClassSpellDTO{}
   var batchDTOBuffer []*dtos.ClassSpellDTO
   var serviceBuffer []*types.ClassSpell
   
   //If neither a single item nor a collection can be bound to JSON, fail early
   //ShouldBindBodyWith is used instead of ShouldBindJSON since the latter prevents multiple bind attempts
   if err := context.RequestContext.ShouldBindBodyWith(DTOBuffer, binding.JSON); err == nil {
      
      serviceBuffer = []*types.ClassSpell{context.DTOFlattener(DTOBuffer)}
      serviceReturn, err := context.ServiceAction(&contextProviders.SaveArgs[types.ClassSpell]{ Items: serviceBuffer })
      if err != nil {
         context.RequestContext.IndentedJSON(http.StatusInternalServerError, err.Error())
         return
      }
      
      returnBuffer, err := context.DTOConverter(serviceReturn[0])
      if err != nil {
         context.RequestContext.IndentedJSON(http.StatusInternalServerError, err.Error())
         return
      }
      
      if (returnBuffer == nil) {
         context.RequestContext.IndentedJSON(http.StatusInternalServerError, errors.New("DTO conversion resulted in nil for object of type ClassSpell"))
         return
      }
      
      context.RequestContext.IndentedJSON(http.StatusOK, returnBuffer)
      return
      
   } else if err := context.RequestContext.ShouldBindBodyWith(&batchDTOBuffer, binding.JSON); err == nil {
      
      serviceBuffer = utils.Map(batchDTOBuffer, func(dto *dtos.ClassSpellDTO) *types.ClassSpell { return context.DTOFlattener(dto) })
      serviceReturn, err := context.ServiceAction(&contextProviders.SaveArgs[types.ClassSpell]{ Items: serviceBuffer })
      if err != nil {
         context.RequestContext.IndentedJSON(http.StatusInternalServerError, err.Error())
         return
      }
      
      returnBuffer, err := utils.ErrorCompatibleMap(serviceReturn, func(dbReturn *types.ClassSpell) (*dtos.ClassSpellDTO, error) { return context.DTOConverter(dbReturn) })
      
      if err != nil {
         context.RequestContext.IndentedJSON(http.StatusInternalServerError, err.Error())
         return
      }
      
      context.RequestContext.IndentedJSON(http.StatusOK, returnBuffer)
      return
      
   } else {
      context.RequestContext.IndentedJSON(http.StatusBadRequest, err.Error())
      return
   }
}
