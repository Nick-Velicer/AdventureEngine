//*** CAUTION ***
//This is an autogenerated file produced by regenerateBackend.py.
//Edits made here will not persist after backend regeneration.

package generatedservices
import (
   "errors"
   "reflect"
   types "AdventureEngineServer/generatedTypes"
   "gorm.io/gorm"
)

func GetCharacters(db *gorm.DB, characters *[]types.CharacterJson) error {
   result := db.Find(characters)
   return result.Error
}

func GetCharacterById(db *gorm.DB, id int, character *types.CharacterJson) error {
   result := db.First(character, id)
   return result.Error
}

func SaveCharacter(db *gorm.DB, character *types.CharacterJson) error {
   tx := db.Begin()
   
   if tx.Error != nil {
   return errors.New("Could not initialize transaction to save " + reflect.TypeOf(character).Name() + " entity")
   }
   
   defer func() {
      if r := recover(); r != nil {
         tx.Rollback()
      }
   }()
   
   if err := tx.Error; err != nil {
      return err
   }
   
   if err := tx.Save(character).Error; err != nil {
      tx.Rollback()
      return err
   }
   
   return tx.Commit().Error
}
