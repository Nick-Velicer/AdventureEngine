//*** CAUTION ***
//This is an autogenerated file produced by regenerateBackend.py.
//Edits made here will not persist after backend regeneration.

package generatedservices
import (
   "errors"
   "reflect"
   types "AdventureEngineServer/generatedTypes"
   "gorm.io/gorm"
)

func GetDomainSpeciess(db *gorm.DB, domainSpeciess *[]types.DomainSpeciesJson) error {
   result := db.Find(domainSpeciess)
   return result.Error
}

func GetDomainSpeciesById(db *gorm.DB, id int, domainSpecies *types.DomainSpeciesJson) error {
   result := db.First(domainSpecies, id)
   return result.Error
}

func SaveDomainSpecies(db *gorm.DB, domainSpecies *types.DomainSpeciesJson) error {
   tx := db.Begin()
   
   if tx.Error != nil {
   return errors.New("Could not initialize transaction to save " + reflect.TypeOf(domainSpecies).Name() + " entity")
   }
   
   defer func() {
      if r := recover(); r != nil {
         tx.Rollback()
      }
   }()
   
   if err := tx.Error; err != nil {
      return err
   }
   
   if err := tx.Save(domainSpecies).Error; err != nil {
      tx.Rollback()
      return err
   }
   
   return tx.Commit().Error
}
