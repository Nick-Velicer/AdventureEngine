//*** CAUTION ***
//This is an autogenerated file produced by regenerateBackend.py.
//Edits made here will not persist after backend regeneration.

package generatedservices
import (
   "errors"
   "reflect"
   types "AdventureEngineServer/generatedTypes"
   "gorm.io/gorm"
)

func GetDomainCreatureTypes(db *gorm.DB, domainCreatureTypes *[]types.DomainCreatureTypeJson) error {
   result := db.Find(domainCreatureTypes)
   return result.Error
}

func GetDomainCreatureTypeById(db *gorm.DB, id int, domainCreatureType *types.DomainCreatureTypeJson) error {
   result := db.First(domainCreatureType, id)
   return result.Error
}

func SaveDomainCreatureType(db *gorm.DB, domainCreatureType *types.DomainCreatureTypeJson) error {
   tx := db.Begin()
   
   if tx.Error != nil {
   return errors.New("Could not initialize transaction to save " + reflect.TypeOf(domainCreatureType).Name() + " entity")
   }
   
   defer func() {
      if r := recover(); r != nil {
         tx.Rollback()
      }
   }()
   
   if err := tx.Error; err != nil {
      return err
   }
   
   if err := tx.Save(domainCreatureType).Error; err != nil {
      tx.Rollback()
      return err
   }
   
   return tx.Commit().Error
}
